{% set name = "tzinfo" %}
{% set version = "2.0.3" %}

package:
  name: rb-{{ name|lower }}
  version: {{ version }}

source:
  url: https://rubygems.org/downloads/{{ name }}-{{ version }}.gem
  sha256: 6f1705dd3ca4cf21fbdd1abdea8338334ef115df2e33e42a8f791b19db9765ab

build:
  noarch: generic
  number: 0
  script:
    - gem install -N -l -V --norc --ignore-dependencies {{ name }}-{{ version }}.gem
    - gem unpack {{ name }}-{{ version }}.gem
  skip: {{ win }}

requirements:
  host:
    - ruby
  run:
    - ruby
    - rb-thread_safe >=0.1,<1
    - rb-concurrent-ruby

test:
  commands:
    - ruby -r {{ name }} -e 'exit 0'

about:
  home: https://rubygems.org/gems/{{ name }}
  license: MIT
  license_family: MIT
  license_file: {{ name }}-{{ version }}/LICENSE
  summary: TZInfo provides access to time zone data and allows times to be converted using time zone rules.

  doc_url: https://www.rubydoc.info/gems/{{ name }}
  dev_url: https://github.com/tzinfo/{{ name }}

extra:
  recipe-maintainers:
    - sodre
